// Mock data removed. Frontend now uses real backend API only.
// Kept empty exports to avoid breaking imports in case some files still reference them.

import { Question, QuestionType, Exam } from '../types';

export const INITIAL_QUESTIONS: Question[] = [];

export const INITIAL_EXAMS: Exam[] = [];
